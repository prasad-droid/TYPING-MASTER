const typing_text = document.querySelector(".type")

function randompara(){

    let randomtext = Math.floor(Math.random * paragraph.length);

    console.log(paragraph[randomindex])
    

        // let spantag=`<span>${span} </span>`;
        // typing_text.innerHTML+= spantag})
}
randompara()